Dato il file divina.txt, scrivere un programma in C che:

- conti le "parole" (ovvero le sequenze alfanumeriche separate da spaziatura) presenti nel file
- allochi opportuno array e ne memorizzi il contenuto supponendo che nessuna parola sia piu' lunga di 50 caratteri. Si usi sempre allocazione dinamica, anche per i singoli elementi
- usi la funzione qsort() per ordinare tale array

fseek()

       int fseek(FILE *stream, long offset, int whence);

fseek(fp, 0, SEEK_SET);
